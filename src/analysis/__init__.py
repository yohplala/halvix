"""
Analysis subpackage: filtering, cycle-point detection, and projection models.

Callers should import from the specific submodules (``analysis.filters``,
``analysis.cycle_points``, ``analysis.cycle_patterns``,
``analysis.point_detection``, ``analysis.projections``) rather than from
the package root — that is the only style used throughout the codebase
and tests. This file is kept intentionally empty so the package surface
mirrors actual usage, matching the convention used by ``data/__init__.py``.
"""
